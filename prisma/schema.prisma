generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model SquozeMetdata {
  id        Int @id @default(autoincrement())
  httpCode  Int    
  createdAt DateTime
  header    String?
}